import React from 'react'

function ProductDetailsComponent({title,body}) {
    
    return (
        <div>
            <p>{title}</p>
            <p>{body}</p>
        </div>
    )
}

export default ProductDetailsComponent
